# 系统诊断和测试指南

## 🎯 当前状态检查

让我们一步步测试系统是否正常工作。

## 1. 服务状态检查

### 后端服务
```bash
# 检查后端是否运行
curl http://localhost:3000/health

# 预期输出：
# {"status":"ok","timestamp":"..."}
```

### 前端服务
```bash
# 检查前端是否运行
curl http://localhost:5173/ | head -5

# 预期输出：HTML内容
```

### MySQL数据库
```bash
# 检查MySQL是否运行
mysqladmin ping

# 预期输出：mysqld is alive
```

## 2. 测试添加股票API

### 步骤1: 先登录获取token

```bash
# 注册或登录
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "123456"
  }'

# 或者登录
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "123456"
  }'
```

保存返回的token。

### 步骤2: 测试添加持仓

```bash
# 替换 YOUR_TOKEN 为实际token
curl -X POST http://localhost:3000/api/portfolio/positions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "stockCode": "000001",
    "stockName": "平安银行",
    "quantity": 1000,
    "costPrice": 10.50,
    "currentPrice": 11.20,
    "category": "left"
  }'
```

## 3. 常见问题排查

### 问题1: 用户未登录

**症状**: 401 Unauthorized错误

**解决**: 
1. 先注册或登录
2. 保存token
3. 在请求头中添加Authorization

### 问题2: 字段验证失败

**症状**: 400 Bad Request

**检查**:
1. 确保所有必填字段都已填写
2. quantity必须是正整数
3. price必须是正数
4. category必须是: left, right, defensive, observation

### 问题3: 数据库错误

**症状**: 500 Internal Server Error

**检查**:
```bash
# 查看后端日志
tail -f /tmp/backend.log

# 检查数据库连接
mysql -u root -e "SELECT 1"
```

### 问题4: 前端请求失败

**检查浏览器控制台**:
1. 按F12打开开发者工具
2. 查看Console标签
3. 查看Network标签
4. 检查API请求的状态码

## 4. 前端测试步骤

1. 访问 http://localhost:5173/
2. 注册或登录账号
3. 进入"持仓管理"页面
4. 点击"添加持仓"按钮
5. 填写表单并提交
6. 查看浏览器控制台是否有错误

## 5. 详细的错误日志位置

### 后端日志
```bash
tail -f /tmp/backend.log
```

### 前端日志
浏览器控制台 (F12)

### 数据库日志
```bash
tail -f /opt/homebrew/var/mysql/hanagardeMac-mini.local.err
```

## 6. 如果添加仍然失败

请提供以下信息：

1. **浏览器控制台错误**（完整截图）
2. **网络请求详情**（Network标签）
3. **后端日志**（最近的错误）
4. **具体操作步骤**（你做了什么操作）

## 7. 快速重置

如果系统有问题，可以尝试重置：

```bash
# 清理所有进程
pkill -f "tsx watch"
pkill -f "vite"

# 重新启动
cd backend && npm run dev &
cd frontend && npm run dev &
```

## 8. 验证清单

- [ ] 后端服务运行在3000端口
- [ ] 前端服务运行在5173端口
- [ ] MySQL数据库运行正常
- [ ] 可以访问 http://localhost:5173/
- [ ] 可以登录/注册
- [ ] 可以添加股票持仓
- [ ] 持仓列表正确显示

## 需要帮助？

请提供：
1. 你点击了什么操作
2. 看到了什么错误信息
3. 浏览器控制台的完整截图

