# 🚀 现在开始安装！

## 步骤 1：安装 Homebrew

### 在终端中运行（复制粘贴以下命令）：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### 安装过程说明：

1. **下载安装脚本**（约 1-2 分钟）
2. **需要输入管理员密码**（您的 Mac 登录密码）
3. **安装 Homebrew**（约 3-5 分钟）
4. **按照提示添加到 PATH**

### 安装完成后，运行：

```bash
# 验证安装
brew --version

# 应该显示类似: Homebrew 4.x.x
```

---

## 步骤 2：安装 MySQL 和 Redis

安装完 Homebrew 后，运行：

```bash
# 方法A：使用自动脚本
./install_dependencies.sh

# 方法B：手动安装
brew install mysql redis
brew services start mysql
brew services start redis
```

---

## 📝 完整命令清单

**复制到终端一条一条执行**：

```bash
# 1. 安装 Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. 验证安装
brew --version

# 3. 安装 MySQL
brew install mysql

# 4. 启动 MySQL
brew services start mysql

# 5. 设置 MySQL 密码
mysql_secure_installation

# 6. 安装 Redis
brew install redis

# 7. 启动 Redis
brew services start redis

# 8. 验证 Redis
redis-cli ping
```

---

## ✅ 完成后告诉我

安装完成后，运行以下命令验证：

```bash
brew --version
mysql --version
redis-server --version
```

如果都显示版本号，就告诉我**"安装完成"**，我继续下一步！

---

## ⏱️ 预计时间

- Homebrew: 5-10 分钟
- MySQL: 3-5 分钟
- Redis: 1-2 分钟
- **总计**: 约 10-20 分钟

---

**开始吧！先运行第一条命令！** 🚀

