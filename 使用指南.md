# 🎵 股票决策小程序 - 使用指南

## 🚀 快速开始（三步搞定）

### 第一步：安装基础依赖

```bash
# 给脚本添加执行权限（如果还没有）
chmod +x install_dependencies.sh setup.sh start.sh

# 安装 MySQL 和 Redis
./install_dependencies.sh
```

**如果没有 Homebrew** (macOS 用户)：
```bash
# 先安装 Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 然后运行安装脚本
./install_dependencies.sh
```

---

### 第二步：配置环境

```bash
# 配置数据库和环境变量
./setup.sh
```

脚本会提示您：
1. 输入 MySQL root 密码
2. 自动创建数据库
3. 生成配置文件
4. 安装项目依赖

---

### 第三步：启动服务

```bash
# 一键启动前后端服务
./start.sh
```

访问地址：
- 📱 前端: http://localhost:5173
- 🔧 后端: http://localhost:5000

---

## 📋 详细步骤

### 环境要求

- ✅ Node.js >= 18.0.0
- ✅ MySQL >= 8.0
- ✅ Redis >= 6.0
- ✅ 操作系统: macOS / Linux / Windows

---

### 完整安装流程

#### 1️⃣ 安装 Homebrew (仅 macOS)

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

#### 2️⃣ 安装 MySQL 和 Redis

**macOS**:
```bash
brew install mysql redis
brew services start mysql
brew services start redis
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt-get update
sudo apt-get install mysql-server redis-server
sudo systemctl start mysql
sudo systemctl start redis
```

**Linux (CentOS/RHEL)**:
```bash
sudo yum install mysql-server redis
sudo systemctl start mysqld
sudo systemctl start redis
```

#### 3️⃣ 配置数据库

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE stock_decision CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

#### 4️⃣ 配置环境变量

```bash
cd backend
cp env.example .env
```

编辑 `backend/.env` 文件：
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=stock_decision
REDIS_URL=redis://localhost:6379
JWT_SECRET=随机生成的密钥
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
```

#### 5️⃣ 安装依赖

```bash
# 后端依赖
cd backend
npm install

# 前端依赖
cd ../frontend
npm install
```

#### 6️⃣ 启动服务

**方法一：使用脚本（推荐）**
```bash
cd ../  # 回到项目根目录
./start.sh
```

**方法二：手动启动**
```bash
# 终端1 - 后端
cd backend
npm run dev

# 终端2 - 前端
cd frontend
npm run dev
```

---

## 🎯 功能演示

### 登录页面
1. 访问 http://localhost:5173
2. 首次使用需要注册账号
3. 使用用户名/邮箱登录

### 我的歌单 (Dashboard)
- 🎵 音乐播放器风格的股票展示
- ▶️ 播放/暂停股票分析
- 📊 投资概览统计

### 持仓管理 (Portfolio)
- 🎶 歌单视图和列表视图
- 🔍 搜索和筛选功能
- ➕ 添加/编辑持仓

### 数据分析 (Analysis)
- 📈 技术分析图表
- 📊 基本面分析
- 🏢 板块分析

### 智能报表 (Reports)
- 🌅 盘前准备报表
- 📈 盘中观察报表
- 🌆 收盘复盘报表
- 🤖 日终决策报表

### AI决策 (Decisions)
- 🧠 智能投资决策
- 📋 决策历史记录
- ⚡ 一键执行决策

### 系统设置 (Settings)
- ⚙️ 个人设置
- 📊 系统监控
- 💬 用户反馈

---

## 🐛 常见问题

### Q1: 启动脚本报错 "command not found: brew"

**原因**: 未安装 Homebrew

**解决**:
```bash
# 安装 Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 重新运行安装脚本
./install_dependencies.sh
```

---

### Q2: MySQL 连接失败

**原因**: MySQL 服务未启动或密码错误

**解决**:
```bash
# 检查 MySQL 是否运行
brew services list | grep mysql

# 启动 MySQL
brew services start mysql

# 测试连接
mysql -u root -p
```

---

### Q3: Redis 连接失败

**原因**: Redis 服务未启动

**解决**:
```bash
# 检查 Redis 是否运行
brew services list | grep redis

# 启动 Redis
brew services start redis

# 测试连接
redis-cli ping
```

---

### Q4: 端口被占用

**原因**: 5000 或 5173 端口已被其他程序使用

**解决**:
```bash
# 查找占用端口的进程
lsof -i :5000
lsof -i :5173

# 杀死进程
kill -9 <PID>

# 或者在配置文件中修改端口
# backend/.env 中修改 PORT=5000
# frontend/vite.config.ts 中修改端口
```

---

### Q5: 依赖安装失败

**原因**: 网络问题或 npm 缓存问题

**解决**:
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install
```

---

### Q6: 前端页面空白

**原因**: 后端服务未启动或 API 地址配置错误

**解决**:
1. 检查后端服务是否运行：http://localhost:5000/health
2. 检查浏览器控制台错误
3. 确认 `frontend/src/services/api.ts` 中的 API 地址正确

---

## 🔧 高级配置

### 修改端口

**后端端口** (编辑 `backend/.env`):
```env
PORT=5000
```

**前端端口** (编辑 `frontend/vite.config.ts`):
```typescript
export default defineConfig({
  server: {
    port: 5173
  }
})
```

### 数据库配置

**远程数据库** (编辑 `backend/.env`):
```env
DB_HOST=your-db-host.com
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=stock_decision
```

### Redis 配置

**远程 Redis** (编辑 `backend/.env`):
```env
REDIS_URL=redis://your-redis-host.com:6379
```

---

## 📞 技术支持

如果遇到问题：

1. 📖 查看日志文件
2. 🔍 检查浏览器控制台
3. 📝 查看错误信息
4. 💬 联系技术支持

---

## 🎉 开始使用

现在您可以：

1. ✅ 访问 http://localhost:5173
2. ✅ 注册/登录账号
3. ✅ 添加持仓股票
4. ✅ 查看数据分析
5. ✅ 生成智能报表
6. ✅ 获取 AI 决策建议

**让投资变得像听音乐一样轻松！** 🎵📈

